<% if flash[:notice] %>
    <div class="notice"><%= flash[:notice] %></div>
<% end %>

<fieldset>
  <legend>Редактирование участников</legend>

  <%= render :partial => 'form' %>

</fieldset>

<br />

<%= link_to "Удалить участника", admin_member_path, :method => :delete,
            data:{:confirm => "Вы уверены" }%> <br />
<%= link_to "Вернуться" , :action => 'index'  %>